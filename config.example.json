{
    "provider": "bunny", // only "bunny" is supported currently
    "api_key": "your_api_key_here",
    "zones": [ // List of zones to manage
        {
            "name": "example.com",
            "records": [
                {
                    "type": "A",
                    "name": "www", // for example this renders to www.example.com
                    "id": 1234567 // record id if available, if it isnt available we will match only based on the name and type
                }
            ]
        }
    ]
}
